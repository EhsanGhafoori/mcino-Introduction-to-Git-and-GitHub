{
  "buildCommand": "dotnet publish -c Release -o published && cp -r published/wwwroot/* ./dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
